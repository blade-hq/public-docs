# Older pnpm versions used by some CI providers require an explicit
# packages field, even for a single-package repository.
packages:
  - '.'

allowBuilds:
  esbuild: true
  sharp: true
minimumReleaseAgeExclude:
  - starlight-links-validator@0.25.3
